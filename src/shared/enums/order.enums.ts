export enum OrderStatusEnum {
    PENDING = 'pending',
    PAID = 'paid',
    CONFIRMED = 'confirmed',
    FAILED = 'failed',
    REFUNDED = 'refunded',
    CANCELLED = 'cancelled',
    FULFILLED = 'fulfilled',
    //     PROCESSING = 'processing',
    //     SHIPPED = 'shipped',
    //     DELIVERED = 'delivered',
}
